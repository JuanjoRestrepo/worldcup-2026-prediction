name: "worldcup_2026"
version: "1.0.0"
config-version: 2

profile: "worldcup_2026"

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"
  - "logs"

models:
  worldcup_2026:
    +materialized: view
    bronze:
      +schema: bronze
    silver:
      +schema: silver
    gold:
      +schema: gold
