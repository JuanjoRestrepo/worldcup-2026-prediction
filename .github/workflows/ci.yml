name: CI Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  validate:
    name: Tests, Lint, Type-check & dbt Validation
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: worldcup
          POSTGRES_PASSWORD: worldcup_dev
          POSTGRES_DB: worldcup_db
        options: >-
          --health-cmd "pg_isready -U worldcup -d worldcup_db"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    env:
      POSTGRES_HOST: localhost
      POSTGRES_PORT: 5432
      POSTGRES_DB: worldcup_db
      POSTGRES_USER: worldcup
      POSTGRES_PASSWORD: worldcup_dev
      DBT_BASE_SCHEMA: analytics

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version-file: .python-version

      - name: Set up uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install PostgreSQL client
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y postgresql-client curl

      - name: Sync project dependencies
        run: uv sync

      - name: Initialize database schema
        env:
          PGPASSWORD: worldcup_dev
        run: |
          psql \
            -h localhost \
            -U worldcup \
            -d worldcup_db \
            -f docker/postgres/init.sql

      # ── Code quality checks (lint before tests to fail fast) ──────────────
      - name: Run Ruff lint
        run: uv run python -m ruff check .

      - name: Run Ruff format check
        run: uv run python -m ruff format --check .

      - name: Run MyPy type checking
        run: uv run python -m mypy src/api src/modeling src/database src/contracts

      # ── Tests ─────────────────────────────────────────────────────────────
      - name: Run pytest
        run: uv run python -m pytest tests/ -v --tb=short

      # ── dbt validation ────────────────────────────────────────────────────
      - name: Validate dbt project syntax
        run: uv run python scripts/run_dbt.py parse

      # ── Failure notification ──────────────────────────────────────────────
      - name: Comment PR with failure details
        if: failure() && github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '❌ CI pipeline failed.\n\nThe blocking validation suite (lint → format → types → tests → dbt) did not pass. Please review the workflow logs before merging.'
            })
